# CLI Guidance

- `bin/aicc.js` is the cleanest wrapper for common monorepo tasks when it already covers the requested workflow.
- Check root `package.json` alongside this CLI before adding new commands.
- Article CRUD commands assume a reachable backend via `AICC_API_URL`.
