apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "agentic-ai.fullname" . }}-config
  labels:
    {{- include "agentic-ai.labels" . | nindent 4 }}
data:
  {{- range $key, $val := .Values.env }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
